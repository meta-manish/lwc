<apex:page standardController="Student__c">
    <apex:pageBlock >
        <apex:pageBlockTable value="{!Student__c}" var="student">
            <apex:column value="{!student.Name}"/>
            <apex:column value="{!student.Class__r.Name}" headerValue="Class"/>
        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>